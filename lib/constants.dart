enum FileMediaType { image, video, gif, audio, other }

//region Constants that represent the value of the above enum (realm-dart doesn't support enums yet. See https://github.com/realm/realm-dart/issues/681)
const imageType = 0;
const videoType = 1;
const gifType = 2;
const audioType = 3;
const otherType = 4;
//endregion
